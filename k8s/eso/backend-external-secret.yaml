apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: back-secrets-mapping
  namespace: pro-shop
spec:
  refreshInterval: 15m
  secretStoreRef:
    name: aws-secrets
    kind: ClusterSecretStore
  target:
    name: proshop-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: /prod/back/credentials