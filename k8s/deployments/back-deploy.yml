apiVersion: apps/v1
kind: Deployment
metadata:
  name: back-deploy
  labels:
    app: proshop
    env: production
    version: v1.0
    tier: backend
  annotations:
    maintainer: "Team 274"
    description: "Deployment for ProShop backend application"
  
spec:
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: ghcr.io/nhahub/nha-274/backend:cd0f9f4c3d2bf16884a5ef22e18053262e735792
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5000
          resources:
            requests:
              cpu: "350m"      
              memory: "200Mi"  
            limits:
              cpu: "700m"      
              memory: "400Mi"  

        
          envFrom:
            - secretRef:
                name: proshop-secret
